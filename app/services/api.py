# User creates a draft of a trade
# Validator checks Action fields
# State of trade is in draft
# User_id exists ? assumption that this user is logged in
# Validator checks trade details object
# do we need to validate trade_id
# date fields are valid dates and in <=<=<=
# when should we add to trades object ?
# Draft time
# Update time
# Booked time
